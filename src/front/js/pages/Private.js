import React from "react";

const Private = ({ obra }) => {
  return <div className="container text-center">
    <h1>Solo clientes VIP</h1>
    <div className="mt-5 text-danger">Gracias por loguiarse. Esta pagina solo se verá si estas en el sistema.</div>
    </div>;
};

export default Private;
